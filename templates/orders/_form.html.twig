{# <script>
  window.confirmOptIn = function () {
  FB.AppEvents.logEvent('MessengerCheckboxUserConfirmation', null, { 'app_id':
  '2743452592557910', 'page_id': '283620859105921', 'ref': '{{ app.user.id }}',
  'user_ref': '{{ user_ref }}' }); }
</script> #}
{{ form_start(form) }}
{% if form.deliveryDate is defined %}
  {{ form_widget(form.deliveryDate) }}
{% endif %}
{% if form.deliveryTime is defined %}
  {{ form_widget(form.deliveryTime) }}
{% endif %}
<div class="field">
  <label class="label">{{ form_label(form.paymentType) }}</label>
  <div class="control">
    <div class="select">
      {{ form_widget(form.paymentType) }}
    </div>
  </div>
</div>
<div class="field">
  <label class="label">{{ form_label(form.comment) }}</label>
  <div class="control">
    {{ form_widget(form.comment) }}
  </div>
</div>
{# <div class="fb-messenger-checkbox"
  origin="https://bigred.one19.nz"
  page_id="283620859105921"
  messenger_app_id="2743452592557910"
  user_ref="{{ user_ref }}"
  allow_login="true"
  size="large"
  skin="light"
  center_align="false"></div> #}
<div>
  <button class="button">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
