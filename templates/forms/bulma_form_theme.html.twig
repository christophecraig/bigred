{% block form_row %}
  <div class="field">
    {{ form_label(form) }}
    {{ form_errors(form) }}
    {{
      form_widget(
        form,
        {
          attr: {
            class: 'input'
          }
        }
      )
    }}
  </div>
{% endblock %}
